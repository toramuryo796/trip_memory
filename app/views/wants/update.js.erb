document.getElementById('program-list-<%= @want.id %>').innerHTML = '<%= j(render @want) %>'
var form = document.getElementById('want-form')
form.outerHTML = '<%= j(render "form", want: Want.new, value: "追加") %>'

document.getElementById('priority-form')
document.getElementById('program-form')