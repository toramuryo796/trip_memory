document.getElementById('dictionary-list-<%= @dictionary.id %>').innerHTML = '<%= j(render @dictionary) %>'
var form = document.getElementById('dictionary-form')
form.outerHTML = '<%= j(render "form", dictionary: Dictionary.new, value: "追加", group_id: @group.id, paln_id: @plan.id) %>'
document.getElementById('language-form').value = ""
document.getElementById('japanese-form').value = ""
document.getElementById('translation-form').value = ""